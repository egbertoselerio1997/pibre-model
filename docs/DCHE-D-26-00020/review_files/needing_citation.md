| # | Statement / Claim (with approximate location) | Why It Needs a Citation | Characteristic of Needed Citation | Google Scholar Keywords |
|---|---|---|---|---|
| 1 | "While linear methods like Partial Least Squares (PLS) offer interpretable mappings, they inherently struggle to capture the severe non-linearities of microbial kinetics without extensive feature engineering." (Intro, ¶2) | Sweeping claim about PLS limitations in nonlinear systems. PLS is cited generically (Wold et al.) but nothing substantiates the claim that it *struggles* with ASM-type kinetics specifically. | Comparative study or review showing PLS underperformance on nonlinear biological/chemical systems | `"partial least squares" AND (nonlinear OR limitations) AND (biological OR kinetics OR "wastewater")` |
| 2 | "Positivity-only output transformations can prevent negative states, but without an accompanying conservation mechanism they do not by themselves enforce global material balance." (Intro, ¶4) | Asserts a general inadequacy of positivity-only methods as a standalone class. No reference accompanies this particular sentence. | Study showing that positivity clipping or softplus/ReLU output layers do not guarantee conservation in scientific ML | `"non-negativity" AND ("mass conservation" OR "conservation law") AND ("neural network" OR "machine learning" OR surrogate)` |
| 3 | "violations of either mass conservation or non-negativity can distort interpretation, confound process optimization, and undermine confidence in surrogate-assisted design" (Intro, ¶6) | Three sweeping consequences asserted without evidence. Each would benefit from a reference documenting the practical impact. | Engineering or optimization study documenting the impact of physically inadmissible surrogate predictions on downstream design or control | `"surrogate model" AND ("physical constraint" OR "mass balance") AND ("process optimization" OR "process design") AND (violation OR infeasible)` |
| 4 | "reduce reliance on ad hoc clipping or reconciliation" (Intro, ¶6) | Implies that ad hoc clipping/reconciliation is a recognized problem in practice but cites no source documenting this practice or its shortcomings. | Study or review where post-hoc clipping or data reconciliation is described as common practice in process modeling or ML surrogates | `("data reconciliation" OR "post-hoc clipping" OR "output clipping") AND ("surrogate model" OR "machine learning") AND ("wastewater" OR "chemical engineering")` |
| 5 | "no prior activated sludge surrogate has provided hard guarantees of both stoichiometric mass conservation and componentwise non-negativity within a single interpretable steady-state framework" (Intro, ¶6) | Strong novelty/gap claim. To defend it, a systematic review or broader survey reference would strengthen credibility and demonstrate due diligence. | Review or survey of ML/surrogate approaches in wastewater treatment that inventories what constraints have been applied | `"activated sludge" AND "surrogate" AND ("mass conservation" OR "stoichiometric") AND ("review" OR "survey") AND "machine learning"` |
| 6 | "Operational variables $u$ alter the process environment, whereas influent concentrations $c_{in}$ determine the material inventory presented to that environment." (Sec 2.1) | Presented as a self-evident engineering fact, but the conceptual separation of manipulated variables vs. loading variables in WWT modeling is a modeling convention that has been discussed in the literature. | Textbook or process-modeling reference that distinguishes operational degrees of freedom from influent loading in biological treatment design | `"activated sludge" AND ("operational variables" OR "manipulated variables") AND ("influent" OR "loading") AND (modeling OR design)` |
| 7 | "Couplings among dissolved oxygen and nitrifier channels, among ammonium, nitrite, and nitrate, among $S_{PO4}$, $X_{PAO}$, $X_{PP}$, and $X_{PHA}$… correspond to recurring material linkages that operators already recognize in nitrification, denitrification, phosphorus storage, and hydrolysis-uptake behavior." (Sec 2.5) | Sweeping assertion about what wastewater operators "already recognize." Needs process-engineering citations documenting these well-known coupling pathways. | Biological nutrient removal reference(s) describing nitrification/denitrification coupling, EBPR PAO-PP-PHA metabolism, and dissolved-oxygen interactions | `"biological nutrient removal" AND ("nitrification" OR "denitrification") AND ("phosphorus accumulating organisms" OR "EBPR") AND ("coupling" OR "interaction")` |
| 8 | "each exact block update cannot increase the coupled objective, so the objective sequence within a restart is monotone non-increasing and converges in value to a stationary point" (Sec 2.6) | Well-known block coordinate descent convergence result, but stated without attribution. Reviewers may expect a reference to the foundational optimization theory. | Optimization textbook or paper proving convergence of block-coordinate minimization with exact block solves | `"block coordinate descent" AND (convergence OR "monotone decrease") AND ("stationary point" OR "convex optimization")` |
| 9 | "Stochastic methods such as Adam, and indeed other generic first-order solvers, do not provide that admissibility guarantee by themselves. They may lower the objective while still visiting or returning nearly singular coupling matrices unless an additional conditioning projection is imposed externally." (Sec 2.6) | Broad claim about Adam and all first-order optimizers lacking constraint admissibility. Needs a reference documenting constraint violations in stochastic training or discussing the need for explicit feasibility projections. | Study on constrained optimization with SGD/Adam showing that unconstrained gradient steps can violate structural constraints | `"Adam optimizer" AND ("constraint violation" OR "feasibility" OR "projection") AND ("stochastic gradient" OR "first-order") AND ("constrained optimization")` |
| 10 | "OSQP is used for the convex QP subproblems" (Sec 2.6) | Software tool used as a core solver component but not cited. Reviewers and readers need the software reference. | OSQP software paper | `OSQP AND "operator splitting" AND "quadratic program"` |
| 11 | "solved with HiGHS" (Sec 2.6) | Software tool used for the deployment LP but not cited. | HiGHS software paper | `HiGHS AND "linear programming" AND solver` |
| 12 | "Latin hypercube sampling with random seed 42" (Sec 3.2) | Standard DOE method used without any methodological citation. | Original or canonical reference for Latin hypercube sampling | `"Latin hypercube sampling" AND ("experimental design" OR "space-filling")` |
| 13 | "dynamically relaxed for 20 days with a stiff backward differentiation formula integrator" (Sec 3.2) | BDF integration method used but not cited. If using SciPy's `solve_ivp` with BDF, the solver or the BDF method origin should be cited. | BDF integrator method reference or the numerical library used | `"backward differentiation formula" AND (stiff OR integrator) AND ("ordinary differential equations")` |
| 14 | "promoted biomass pools that are known to persist across nearby operating conditions" (Sec 3.2) | Claimed as "known" behavior without any reference to support the assertion that biomass pools persist across operating regimes. | WWT or biokinetics study demonstrating slow biomass decay / persistence of heterotrophs, PAO, or nitrifiers under varying operating conditions | `"activated sludge" AND ("biomass persistence" OR "slow decay" OR "biomass retention") AND ("operating conditions" OR "steady state")` |
| 15 | "using a seeded tree-structured Parzen estimator sampler" (Sec 3.4) | TPE sampler used for hyperparameter optimization, and Optuna is the platform, but neither is cited. | Optuna software paper and/or TPE method reference | `Optuna AND "hyperparameter optimization" AND ("tree-structured Parzen estimator" OR TPE)` |
| 16 | "standardized by z-score transformations fit on the training split only" (Sec 3.5) | Standard preprocessing practice stated as convention without citation. A reviewer may expect a reference to the rationale for fitting scalers only on training data. | ML best-practice reference or textbook discussing data leakage prevention via train-only standardization | `"feature scaling" AND ("training set" OR "data leakage") AND ("standardization" OR "z-score") AND "machine learning"` |
| 17 | "the structural restrictions imposed by ICSOR tend to reduce overfitting relative to the unconstrained baselines without a proportionate loss in predictive accuracy" (Sec 4.2) | Invokes the well-known regularization/inductive-bias principle that constraints reduce overfitting, but provides no theoretical or empirical citation. | Statistical learning theory or physics-informed ML reference showing that physically motivated constraints act as regularizers and reduce generalization error | `"physics-informed" AND ("regularization" OR "inductive bias" OR "generalization") AND ("overfitting" OR "bias-variance") AND "machine learning"` |
| 18 | "A surrogate may thus appear accurate in the reported composite space while producing component-level states that are not physically admissible." (Sec 4.3) | Sweeping generalization about surrogate behavior beyond this study's specific results. Would be strengthened with a reference showing this phenomenon in other domains. | Study in atmospheric chemistry, reaction engineering, or WWT where aggregate metrics mask component-level physical violations | `"surrogate model" AND ("physical admissibility" OR "physical consistency") AND ("component" OR "species") AND (violation OR infeasible)` |
| 19 | "second-order COD behavior in this reactor is associated primarily with the redox environment and nitrifier-linked loading interactions rather than with a broad field of carbon-only cross terms" (Sec 4.4) | Process-engineering interpretation of fitted coefficients presented without any reference to established knowledge about dissolved-oxygen–nitrifier–COD coupling in activated sludge. | WWT process reference documenting the well-known coupling between dissolved oxygen, nitrification, and COD removal in activated sludge | `"activated sludge" AND "dissolved oxygen" AND ("nitrification" OR "nitrifiers") AND ("COD removal" OR "carbon oxidation") AND interaction` |
| 20 | "The MLP baseline contained three hidden layers of widths 128, 64, and 32, used logistic activation" (Sec 3.4) | Architecture choice presented without justification or reference to guidelines for MLP sizing in surrogate modeling contexts. | Study or guideline on neural network architecture selection for scientific/engineering surrogate models | `"neural network" AND ("architecture selection" OR "hidden layer" OR "network size") AND ("surrogate model" OR "chemical engineering")` |
| 21 | The normalized area under the learning curve (nAUC) metric is defined and used as a primary summary statistic (Sec 3.5) but presented as if it were a standard metric, with no citation establishing prior use. | If this is a novel metric, the text should explicitly state so. If borrowed, the source should be cited. | Prior use of area-under-the-learning-curve as a model comparison metric in ML | `"area under the learning curve" AND ("model comparison" OR "sample efficiency" OR "benchmark") AND "machine learning"` |
| 22 | "dense rank $r_{i,m,k}(n)$… the metric-specific average rank and overall average rank" (Sec 3.5) | The ranking aggregation methodology across targets, metrics, and dataset sizes is presented without citation. Similar rank-aggregation schemes exist in AutoML and ML benchmarking literature. | ML benchmarking paper using rank-aggregation across metrics and datasets | `"average rank" AND ("model comparison" OR "benchmark") AND ("multiple metrics" OR "multiple datasets") AND "machine learning"` |
| 23 | "The ASM2d-TSN implementation follows Guerrero et al. (2011)… all stoichiometric values and simulation rate coefficients used here were taken from that study." (Sec 3.1) — but the two-step nitrification (TSN) extension itself is not separately cited. | Guerrero et al. (2011) is cited for the base model, but the two-step nitrification modification (AOB/NOB splitting) is a distinct modeling choice that may originate from a different source. | Reference for the two-step nitrification extension of ASM2d separating AOB and NOB | `"ASM2d" AND "two-step nitrification" AND ("AOB" OR "NOB" OR "nitrite") AND ("activated sludge model")` |

| # | Statement / Claim (Section) | Why It Needs a Citation | Characteristic of Needed Citation | Google Scholar Keywords |
|---|---|---|---|---|
| 24 | "a single train-test partition does not suffice to characterize sample efficiency or ranking stability" (Sec 4.1) | Well-known ML evaluation principle invoked to justify the repeated-size study, but stated without attribution. A reviewer may ask for the methodological basis. | ML methodology reference on the inadequacy of single train-test splits and the need for repeated evaluation or cross-validation for reliable model comparison | `"model comparison" AND ("single split" OR "repeated evaluation" OR "cross-validation") AND ("variance" OR "stability") AND "machine learning"` |
| 25 | "ICSOR offers comparatively favorable sample efficiency in the low-data regime, but that the more flexible unconstrained baselines can surpass it once sufficient training data are available" (Sec 4.2) | Classic bias-variance / inductive-bias crossover pattern attributed to ICSOR's constraints but never grounded in theory. Distinct from #17 (overfitting reduction): this is the specific claim that constrained models dominate at small $N$ and are overtaken at large $N$. | Statistical learning theory or physics-informed ML study showing that inductive bias from physical constraints yields better sample efficiency at small data sizes but lower asymptotic ceiling | `"inductive bias" AND ("sample efficiency" OR "small data" OR "low data") AND ("physics-informed" OR "constrained model") AND ("bias-variance" OR "learning curve")` |
| 26 | "PLS also exhibits a small gap (0.3824), but its training and held-out errors are both large, consistent with systematic underfitting" (Sec 4.2) | The diagnosis "systematic underfitting" (high bias, low variance) is invoked as an explanatory concept without citing the statistical-learning framework that defines it. | Textbook or foundational reference defining the bias-variance decomposition and the underfitting/overfitting interpretation of train-test gap patterns | `"bias-variance tradeoff" AND ("underfitting" OR "high bias") AND ("train" OR "test" OR "generalization") AND ("machine learning" OR "statistical learning")` |
| 27 | "AdaBoost, Random Forest, and $k$-NN remain nonnegative on the test set, yet all three still violate the stoichiometric invariants on every sample" (Sec 4.3) | The fact that tree-based ensembles and instance-based methods naturally preserve non-negativity (because they interpolate/average observed non-negative targets) is treated as an empirical observation. Citing the structural reason would strengthen the remark. | Reference explaining that tree ensembles, bagging, and $k$-NN predictions are convex combinations of training targets, hence inherit non-negativity when targets are non-negative | `("random forest" OR "ensemble" OR "k-nearest neighbors") AND ("non-negativity" OR "convex combination" OR "interpolation") AND ("prediction" OR "output") AND "machine learning"` |
| 28 | "these results suggest a moderate accuracy-feasibility tradeoff within the present benchmark" (Sec 4.3) | The notion that enforcing physical constraints incurs a predictive-accuracy cost is presented as an empirical finding but framed as a general tradeoff. Prior work has studied this tradeoff in physics-informed ML. | Study quantifying the accuracy cost of hard physical constraints in scientific surrogate models | `"physics-informed" AND ("accuracy" OR "prediction error") AND ("constraint" OR "feasibility") AND ("tradeoff" OR "trade-off") AND ("surrogate" OR "machine learning")` |
| 29 | "the fitted COD driver indicates that second-order COD behavior in this reactor is associated primarily with the redox environment and nitrifier-linked loading interactions" (Sec 4.4) — specifically the interpretation that $S_O$–$S_O$ self-curvature and $S_O$–$S_{N2}$, $S_O$–$S_{NO2}$ cross-terms dominate | The learned coefficient signs and magnitudes are given process-engineering interpretations (oxygen-limited nitrification coupling, redox-COD interaction) but no reference anchors these interpretations to established activated-sludge process knowledge. (Partially overlaps #19 but this is about the *specific coefficient interpretation*, not the general statement.) | Activated sludge process reference documenting that dissolved oxygen is the primary control variable coupling nitrification kinetics to organic carbon removal, and that nitrifier activity creates oxygen demand that competes with heterotrophic COD removal | `"activated sludge" AND "dissolved oxygen" AND ("oxygen demand" OR "oxygen competition") AND ("nitrification" OR "heterotrophic") AND ("COD" OR "carbon")` |
| 30 | "The model therefore compresses nonlinear influent curvature while preserving a broad redistribution layer across ASM components. Overall, 54.3% of the candidate coefficients remain above the reporting threshold" (Sec 4.4) | The observation that the learned model is *structured-sparse* (sparse quadratic blocks, dense coupling) is presented without connecting it to any existing framework for structured sparsity or interpretability in scientific regression. | Reference on structured sparsity or hierarchical sparsity in multivariate regression or scientific ML, showing that block-wise sparsity patterns are meaningful rather than arbitrary | `"structured sparsity" AND ("multivariate regression" OR "multi-output") AND ("interpretability" OR "feature selection") AND ("machine learning" OR "scientific")` |
| 31 | "Presenting the main discussion at the COD-block level therefore highlights the curvature structure that is most relevant to process interpretation" (Sec 4.4) — and the broader claim that coefficient inspection constitutes "interpretability" | The manuscript claims interpretability throughout but never cites a formal framework for what interpretability means in ML. A reviewer may challenge whether coefficient magnitude inspection qualifies. | Reference defining interpretability in ML or scientific modeling, particularly distinguishing intrinsic interpretability (transparent model structure) from post-hoc explanation | `"interpretable machine learning" AND ("definition" OR "taxonomy" OR "framework") AND ("transparent" OR "intrinsic" OR "coefficient") AND ("scientific" OR "regression")` |
| 32 | "remains practical for offline surrogate fitting" (Sec 4.2) — ICSOR's 94.4 s training time is described as practical without any reference framing what computational budget is acceptable for offline surrogate construction | The claim that ~100 s is "practical" is subjective. A citation to surrogate-modeling literature discussing acceptable offline training budgets would anchor it. | Surrogate modeling study or review discussing computational cost thresholds for offline model fitting in chemical or process engineering | `"surrogate model" AND ("computational cost" OR "training time") AND ("offline" OR "process engineering" OR "chemical engineering") AND "practical"` |
| 33 | "Whether this tradeoff remains favorable for more complex reactor configurations, broader operating envelopes, or integration into surrogate-assisted optimization and control workflows remains to be investigated" (Conclusion) | This forward-looking statement names specific future directions (complex reactors, optimization integration) without citing the existing body of work on surrogate-assisted optimization in WWT that motivates them. | Study on surrogate-assisted optimization or model-based control in wastewater treatment that would motivate the stated future directions | `"surrogate-assisted optimization" AND ("wastewater treatment" OR "activated sludge") AND ("reactor" OR "process control" OR "design")` |